<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ltns.rest_area_Schedule.ScheduleDAO">

	<select id="showSchedule" resultType="com.ltns.rest_area_Schedule.ScheduleDTO">
		select
			subject,startDate,endDate,memo
		from
			schedule
		order by
			num
		desc
	</select>
	
	<insert id="addSchedule" parameterType="com.ltns.rest_area_Schedule.ScheduleDTO">
		inert into
			schedule(subject, startDate, endDate, memo)
		values
			(#{subject}, #{startDate}, #{endDate}, #{memo})
	</insert>
	
</mapper>


